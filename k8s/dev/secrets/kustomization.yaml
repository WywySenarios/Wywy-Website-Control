apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# SOPS-encrypted Secret manifests (kustomize-sops decrypts *.sops.yaml
# during ArgoCD repo-server builds; do not commit the decrypted content).
resources:
  - registry-auth.sops.yaml
  - github-runner-pat.sops.yaml
