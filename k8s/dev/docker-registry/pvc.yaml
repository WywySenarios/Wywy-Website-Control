apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: docker-registry-data
  labels:
    app: docker-registry
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 32Gi
