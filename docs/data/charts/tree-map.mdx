---
title: Tree Map
published: 2026-03-15T16:43:28+00:00
---

## Tree Map

The tree map takes in a vector of names, a vector of values, and a vector of ancestry vectors.

### Error Handling

The tree map will gracefully fail if the size of the input vectors do not match.

\@TODO The tree map has a depth limit of 20 and will gracefully fail if it hits this limit.

### Performance

\@TODO The tree map is not optimized for performance for deep ancestry lines. The insertion of every point will traverse through all of their ancestors.
