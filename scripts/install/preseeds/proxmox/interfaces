# /etc/network/interfaces -- generated by build-proxmox-iso.sh
# Managed by ifupdown2

auto lo
iface lo inet loopback

auto <interface>
iface <interface> inet manual
<wifiline>

auto vmbr0
iface vmbr0 inet static
    address <ip>/24
    gateway <gateway>
    dns-nameservers <dns>
    bridge-ports <interface>
    bridge-stp off
    bridge-fd 0
