# VM Installation

This assumes that you don't have Proxmox-level shared storage.

Remember to specify your target: `--dev`, `--prod`, or the IPs you want to target.

Remember to populate `.env.network`.
